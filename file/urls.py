from django.urls import path
from account import views

# 계정관리
urlpatterns = [
    # path('list/', views.ListFilesView.as_view()),
    # path('upload/', views.UploadView.as_view()),
    # path('download/<int:file_id>', views.DownloadView.as_view()),
    # path('delete/<int:file_id>', views.DeleteView.as_view()),
    # path('modify/<int:file_id>', views.ModifyView.as_view()),
    # path('info/<int:file_id>', views.InfoView.as_view()),
    # path('lock/<int:file_id>', views.LockView.as_view()),
]
